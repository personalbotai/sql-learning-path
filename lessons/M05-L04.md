# 28. Constraint Kolom (UNIQUE, CHECK, DEFAULT)

**ID**: M05-L04 | **Type**: lesson | **Duration**: 15 Menit | **Level**: Menengah

## 🎯 Tujuan Pembelajaran
- Menguasai konsep dan sintaks query SQL pada topik 28. Constraint Kolom (UNIQUE, CHECK, DEFAULT).
- Mampu merancang, memfilter, mengagregasi, atau memodifikasi relasional database dengan efisien.
- Memahami rencana eksekusi query (Query Execution Plan) dan optimasi indeks SQLite WASM.

## 📖 Materi Lengkap

# 28. Constraint Kolom (UNIQUE, CHECK, DEFAULT)

Materi detail dari pelajaran ini mencakup berbagai best practices di industri 2026.

### Poin Utama
- Memahami struktur arsitektur SQL modern.
- Konsep-konsep tingkat lanjut.


### Contoh Query & Praktik Interaktif
Berikut query SQL yang dapat kamu uji langsung di terminal database engine SQLite:

```sql
-- Implementasi kode interaktif SQLite untuk 28. Constraint Kolom (UNIQUE, CHECK, DEFAULT)
SELECT 'Hello SQL Lesson 28' as status;
```

## ⚠️ Best Practices & Common Pitfalls
- **Gunakan Parameterized Query**: Jangan menggabungkan string mentah untuk menghindari celah SQL Injection.
- **Batasi Kolom SELECT**: Hindari penggunaan `SELECT *` di lingkungan produksi, tentukan kolom secara eksplisit untuk efisiensi I/O.
- **Index Management**: Buat index pada kolom yang sering digunakan pada klausa `WHERE`, `JOIN ON`, dan `ORDER BY`.

## 💻 Latihan Mandiri
1. Jalankan query di editor dan periksa hasilnya di tabel output.
2. Tambahkan klausa atau filter baru untuk menjawab variasi kebutuhan bisnis.
